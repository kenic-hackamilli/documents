{
  "name": "dotke-documents",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone documents service for FAQ and privacy policy pages.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.mjs"
  }
}
